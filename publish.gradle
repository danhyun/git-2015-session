buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'org.ajoberstar:gradle-git:1.0.0'
  }
}

apply plugin: 'org.ajoberstar.github-pages'

githubPages {
  repoUri = 'git@github.com:danhyun/git-2015-session.git'
  pages {
    from(file("$rootDir/build/asciidoc/revealjs")) {
      into "."
    }
  }
}
