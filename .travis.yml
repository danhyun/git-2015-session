language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.gradle

script:
  - ./gradlew clean asciidoctor -i
  - ./gradlew publish -i -PghToken=${GH_TOKEN}
